import styled from 'styled-components';

export const StyledOverviewCICO = styled.div``;
export const StyledHead = styled.div`
  margin-bottom: 2.5rem;

  p {
    margin: 0;

    &:first-child {
      font-weight: 500;
      font-size: 24px;
      line-height: 32px;
    }

    &:last-child {
      font-weight: 700;
      font-size: 30px;
      line-height: 26px;
    }

    span {
      color: rgb(59 130 246);
    }

    img {
      width: 32px;
      height: 32px;
      left: 8px;
      display: inline;
      bottom: 4px;
      position: relative;
      max-width: 100%;
      vertical-align: middle;
    }
  }
`;
