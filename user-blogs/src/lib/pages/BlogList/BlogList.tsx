import React from "react";
import { BlogListContainer } from "src/lib/containers";

const BlogList = () => {
  return (
    <div>
      <BlogListContainer />
    </div>
  );
};

export default BlogList;
